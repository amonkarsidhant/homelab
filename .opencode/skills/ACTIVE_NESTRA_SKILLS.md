# Active Skills For Nestra

The full `claude-skills` library is installed in this project, but Nestra work should use only the curated stack defined in:

- `nestra/SKILLS_STACK.md`

When running Nestra commands (`/nestra-plan`, `/nestra-feature`, `/nestra-execute`), treat all other imported skills as inactive unless explicitly needed for a special case.
